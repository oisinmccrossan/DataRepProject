import React from 'react';

// Header component definition
const Header = () => {
  // Return the JSX to render the header
  return <h1>My Header in another component</h1>;
};

// Export the Header component as the default export
export default Header;